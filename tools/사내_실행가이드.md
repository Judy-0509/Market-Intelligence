# 사내 엑셀 구조 프로파일링 실행 가이드

TAM 지원 시스템 Phase 1(DB 자동화)을 설계하려면 사내 엑셀들의 구조를 알아야 합니다.
`Get-ExcelProfile.ps1`은 엑셀들을 **읽기 전용**으로 열어 구조 정보만 JSON으로 뽑아냅니다.

- 원본 파일은 수정하지 않음 (읽기 전용 + 매크로 실행 차단 + 외부 링크 갱신 차단)
- 별도 설치 불필요 — Excel이 설치된 Windows PC면 바로 동작
- 추출 내용: 시트 목록/크기, 상단 8행 샘플, 마지막 2행 샘플, 수식 패턴, 열 서식,
  표/피벗/이름정의, 외부 연결(파워쿼리·링크 포함)

## 1. 준비

`Get-ExcelProfile.ps1` 파일을 사내 PC로 복사 (텍스트 복사-붙여넣기로도 가능).

## 2. 실행 — 방법 A: 사내 Claude Code(Sonnet)에 아래 프롬프트 붙여넣기

```
아래 명령을 그대로 실행해줘. 스크립트를 수정하거나 내용을 요약하지 말 것.

powershell -ExecutionPolicy Bypass -File "<스크립트 경로>\Get-ExcelProfile.ps1" -Path "<엑셀 폴더 경로>" -Recurse

실행 후:
1) 콘솔 출력을 그대로 보여주고, 오류가 있으면 오류 메시지 전문을 보여줘.
2) 생성된 excel_profile.json 파일의 전체 경로를 알려줘.
```

`<스크립트 경로>`와 `<엑셀 폴더 경로>`만 실제 경로로 바꾸면 됩니다.

> **대상 선정 팁**: ① 취합용 `tam.xlsx` ② 조사기관 원본 엑셀(Omdia/IDC 등, 기관별 최소 1개씩)
> 이 모두 포함되게 `-Path`를 지정하세요. 폴더가 흩어져 있으면 두 번 실행해서
> `-OutFile profile_tam.json` / `-OutFile profile_기관.json` 처럼 나눠도 됩니다.

## 2. 실행 — 방법 B: PowerShell에서 직접 실행

```powershell
powershell -ExecutionPolicy Bypass -File .\Get-ExcelProfile.ps1 -Path "D:\TAM자료" -Recurse
```

### 주요 옵션

| 옵션 | 기본값 | 설명 |
|---|---|---|
| `-Path` | (필수) | 엑셀 폴더 또는 단일 파일 경로 |
| `-Recurse` | 꺼짐 | 하위 폴더까지 스캔 |
| `-OutFile` | excel_profile.json | 결과 파일명 |
| `-SampleRows` | 8 | 시트 상단 샘플 행 수 |
| `-MaxCols` | 40 | 샘플 최대 열 수 |
| `-MaskNumbers` | 꺼짐 | **기밀 수치 보호**: 숫자를 `#NUM`으로 마스킹 (1900~2100 정수는 연도로 간주해 유지) |

## 3. 전달 전 확인 (보안 체크리스트)

1. `excel_profile.json`을 메모장으로 열어 훑어보기
   - 텍스트 셀 값(고객사명, 프로젝트 코드명 등)이 그대로 들어갑니다
   - 수치가 민감하면 `-MaskNumbers`를 켜고 다시 실행
2. 문제없으면 JSON 파일 통째로 전달 (채팅에 붙여넣기 또는 파일로)
   - 한글이 `\uXXXX` 형태로 이스케이프되어 보여도 정상입니다 (유효한 JSON)
   - 날짜가 45870 같은 숫자(엑셀 시리얼 값)로 보이는 것도 정상입니다

## 알려진 제약

- 암호 걸린 파일은 열지 못하고 JSON에 `error`로 기록됨 (암호 알려주면 옵션 추가 가능)
- `.xlsb`, `.xls` 구형 포맷도 지원 (Excel COM 사용이라 Excel이 여는 건 전부 가능)
